<h2><%= @topic.title %></h2>

<div><%= @topic.content %></div>

<div><%= image_tag @topic.attach.url(:medium) %></div>


<%= link_to("刪除", board_topic_path(@board, @topic), :method => "delete", :confirm => "確定要刪除嗎?" ) %>
<%= link_to("修改", edit_board_topic_path(@board, @topic)) %>